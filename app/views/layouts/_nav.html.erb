<div class="fixed">
  <nav class="top-bar" data-topbar role="navigation">
    <ul class="title-area">
      <li class="name">
        <h1><a href="/">OkNameChanges</a></h1>
      </li>
       <!-- Remove the class "menu-icon" to get rid of menu icon. Take out "Menu" to just have icon alone -->
      <li class="toggle-topbar menu-icon"><a href="#"><span>Menu</span></a></li>
    </ul>

    <section class="top-bar-section">
      <!-- Right Nav Section -->
      <ul class="right">

        <li>
        <% if signed_in? %>
          <%= link_to 'Sign Out', '/users/sign_out', method: 'delete', class: "btn btn-flat btn-primary" %>
        <% else %>
          <%= link_to 'Sign In', "/users/sign_in", class: "btn btn-flat btn-primary" %>
        <% end %>
        </li>
      </ul>

      <!-- Left Nav Section -->
      <ul class="left">
        <% if signed_in? %>
          <li><a href="#">Your Favorites</a></li>
        <% end %>
          <li><a href="#">Popular</a></li>
      </ul>
    </section>
  </nav>
</div>
<br><br>